<h2>New book</h2>

<%= form_with model: Book.new do |f| %>
  <div>
  <label>Title</label>
  <%=f.text_field :title %>
  </div>
  <label>Body</label>
  <%= f.text_area :body %>
  
  <%= f.submit 'Create Book' %>
<% end %>
